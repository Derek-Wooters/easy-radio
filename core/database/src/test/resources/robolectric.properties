# Robolectric 4.16.x emulates up to SDK 35 on JDK 17; emulating SDK 36 would
# require JDK 21. The app still compiles/targets SDK 36 -- this only pins the
# Android runtime the Room DAO tests execute against (minSdk is 26, so 35 is
# fully representative).
sdk=35
