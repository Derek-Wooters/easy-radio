# Robolectric 4.16.x emulates up to SDK 35 on JDK 17; emulating SDK 37 would
# require a newer JDK. The app still compiles/targets SDK 37 -- this only pins
# the Android runtime the Compose UI tests execute against (minSdk is 26, so
# 35 is fully representative).
sdk=35
qualifiers=w411dp-h891dp
